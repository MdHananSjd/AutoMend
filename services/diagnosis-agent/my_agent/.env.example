# AutoMend Diagnosis Agent — Environment Configuration
#
# ⚠️  For zero-cost operation, use AI Studio API key (free tier).
#     Do NOT use Vertex AI (GOOGLE_CLOUD_PROJECT) — that bills per token.
#
# Get your API key from: https://aistudio.google.com/apikey

# Required: AI Studio API key (free tier, zero cost)
GOOGLE_API_KEY=your-ai-studio-api-key-here

# DO NOT SET THESE (they route to Vertex AI which costs money):
# GOOGLE_CLOUD_PROJECT=automend-hackathon
# GOOGLE_CLOUD_LOCATION=us-central1
